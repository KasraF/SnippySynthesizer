{
	"varNames": ["rs"],
	"previous_env": {},
	"envs": [
		{
			"name": "'Kasra Ferdowsi Fard'",
			"rs": "'K.F.F'"
		},
		{
			"name": "'Hello There'",
			"rs": "'H-T'"
		}
	],
	"solutions": [
		"if name.count(\" \") < 2:\n\trs = \"-\".join([var[0] for var in name.split(\" \")])\nelse:\n\trs = \".\".join([var[0] for var in name.split(\" \")])",
		"if name.count(\" \") >= 2:\n\trs = \".\".join([var[0] for var in name.split(\" \")])\nelse:\n\trs = \"-\".join([var[0] for var in name.split(\" \")])"
	]
}