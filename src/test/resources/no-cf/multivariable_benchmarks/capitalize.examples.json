{
	"varNames": [
		"words",
		"caps"
	],
	"envs": [
		{
			"#": "",
			"name": "'alison heck'",
			"words": "['alison', 'heck']",
			"caps": "'Alison Heck'",
			"time": 1
		}
	],
	"solutions": [
		"caps, words = \" \".join([var.capitalize() for var in name.split()]), name.split()",
		"words = name.split(\" \")\ncaps = \" \".join([var.capitalize() for var in words])",
		"words = name.split()\ncaps = \" \".join([var.capitalize() for var in words])"
	],
	"previousEnvs": {
		"1": {
			"name": "'alison heck'"
		}
	}
}