{
	"varNames": [
		"nums",
		"max",
		"min",
		"rs"
	],
	"envs": [
		{
			"inp": "'-2 1 3 5'",
			"nums": "[-2, 1, 3, 5]",
			"max": "5",
			"min": "-2",
			"rs": "'5,-2'",
			"time": 1,
			"#": ""
		}
	],
	"solutions": [
		"nums = [int(var) for var in inp.split(\" \")]\nmax = max(nums)\nmin = min(nums)\nrs = str(max) + \",\" + str(min)",
		"nums = [int(var) for var in inp.split()]\nmax = max(nums)\nmin = min(nums)\nrs = str(max) + \",\" + str(min)",
		"nums = [int(var) for var in inp.split(\" \")]\nmin = min(nums)\nmax = max(nums)\nrs = str(max) + \",\" + str(min)",
		"nums = [int(var) for var in inp.split()]\nmin = min(nums)\nmax = max(nums)\nrs = str(max) + \",\" + str(min)"
	],
	"previousEnvs": {
		"1": {
			"inp": "'-2 1 3 5'"
		}
	}
}