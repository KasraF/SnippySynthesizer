{
	"varNames": [
		"rs"
	],
	"previous_env": {},
	"envs": [
		{
			"line": "'hello world!'",
			"rs": "'olleh !dlrow'"
		},
		{
			"line": "'Sorin Lerner'",
			"rs": "'niroS renreL'"
		}
	],
	"solutions": [
		"rs = \" \".join([var[::-1] for var in line.split(\" \")])",
		"rs = \" \".join([var[::-1] for var in line.split()])",
		"rs = \" \".join(line.split(\" \")[::-1])[::-1]",
		"rs = \" \".join(line.split()[::-1])[::-1]"
	]
}