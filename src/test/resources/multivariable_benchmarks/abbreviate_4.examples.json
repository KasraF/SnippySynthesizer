{
	"varNames": ["words", "chars", "tmp","abbr"],
	"previous_env": {
		"name": "'Augusta Ada King'"
	},
	"envs": [
		{
			"#": "0",
			"name": "'Augusta Ada King'",
			"words": "['Augusta', 'Ada', 'King']",
			"chars": "['A', 'A', 'K']",
			"tmp": "'A.A.K'",
			"abbr": "'a.a.k'"
		}
	],
	"solutions": [
		"words = name.split(\" \")\nchars = [var[0] for var in words]\ntmp = \".\".join(chars)\nabbr = tmp.lower()",
		"words = name.split()\nchars = [var[0] for var in words]\ntmp = \".\".join(chars)\nabbr = tmp.lower()"
	]
}