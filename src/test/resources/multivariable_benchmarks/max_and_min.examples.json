{
	"varNames": [
		"nums",
		"max",
		"min",
		"rs"
	],
	"previous_env": {
		"inp": "'-2 1 3 5'"
	},
	"envs": [
		{
			"inp": "'-2 1 3 5'",
			"nums": "[-2, 1, 3, 5]",
			"max": "5",
			"min": "-2",
			"rs": "'5,-2'"
		}
	],
	"solution": [
		"nums = [int(var) for var in inp.split(\" \")]\nmax = max(num)\nmin = min(nums)\nrs = str(max) + \",\" + str(min)",
		"nums = [int(var) for var in inp.split()]\nmax = max(num)\nmin = min(nums)\nrs = str(max) + \",\" + str(min)",
		"nums = [int(var) for var in inp.split(\" \")]\nmin = min(num)\nmax = max(nums)\nrs = str(max) + \",\" + str(min)",
		"nums = [int(var) for var in inp.split()]\nmin = min(num)\nmax = max(nums)\nrs = str(max) + \",\" + str(min)"
	]
}