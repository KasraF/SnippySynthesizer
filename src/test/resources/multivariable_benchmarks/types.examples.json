{
	"varNames": ["lst", "size"],
	"previous_env": {
		"name": "'Augusta Ada King'"
	},
	"envs": [
		{
			"#": "1",
			"name": "'Augusta Ada King'",
			"lst": "['Augusta', 'Ada', 'King']",
			"size": "3"
		}
	],
	"solutions": [
		"lst = name.split(\" \")\nsize = len(lst)",
		"lst = name.split()\nsize = len(lst)"
	]
}