{
	"varNames": ["words","abbr"],
	"previous_env": {
		"name": "'Augusta Ada King'"
	},
	"envs": [
		{
			"#": "0",
			"name": "'Augusta Ada King'",
			"words": "['Augusta', 'Ada', 'King']",
			"abbr": "'a.a.k'"
		}
	],
	"solutions": [
		"words = name.split()\nabbr = \".\".join([var[0] for var in words]).lower()",
		"words = name.split(\" \")\nabbr = \".\".join([var[0] for var in words]).lower()"
	]
}